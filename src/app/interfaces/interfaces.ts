export interface Componente {
  icon: string;
  name: string;
  redirectTo: string
}

export interface Superheroe {
  superhero: string;
  publisher: string;
  alter_ego: string;
  first_appearance: string;
  characters: string;
}